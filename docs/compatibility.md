# Compatibility & pre-release checklist

Runtime properties that depend on the host Claude Code version. Status is
stated plainly; anything PENDING is a reason this plugin is still in internal
pilot, not a footnote.

| # | Property | Status | Notes |
|---|----------|--------|-------|
| A1 | Per-task model override at spawn (param vs agent frontmatter precedence) | PASSED | 2026-08-18, host 2.1.234. Spawn param takes precedence over frontmatter: `executor.md` declares `model: sonnet`, and `haiku` and `opus` params each won across 4 spawns in 2 independent runs — zero Sonnet reports. Per-tier executor variants not needed. Note: `opus` resolved to the session's variant (`claude-opus-5[1m]`), not a pinned model ID — `model: opus` selects a tier, not an exact build. Evidence is agent self-report against the sonnet frontmatter control — see [verification-log.md](verification-log.md#a1--per-task-model-override-at-spawn-param-vs-agent-frontmatter-precedence). Known limit, still untested: param ignored in agent-teams mode — Drydock uses plain Task spawning. |
| A2 | `isolation: "worktree"` agent spawning | PASSED | 2026-08-18, host 2.1.234, git 2.51.0. Smoke test TEST.1 via `drydock:executor-isolated`, isolation from frontmatter only (no param). (a) Worktree created: `git worktree list` showed a second entry at `.claude/worktrees/agent-<id>` on branch `worktree-agent-<id>`, present on disk. (b) Commit exists in contract format: `6b8ba31` subject exactly `drydock(TEST.1): worktree smoke test`, `1 file changed, 1 insertion(+)`, only the owned file. (c) Both lines present at that commit via `git show 6b8ba31:scratch/test.txt`; main tree correctly still one line at `aa38357`, unmerged. Worktree is **not** auto-removed when it contains changes — cleanup is the orchestrator's job. `.claude/` must be gitignored or it shows as untracked noise in every wavecheck diff. See [verification-log.md](verification-log.md#a2--isolation-worktree-agent-spawning). |
| A2b | Post-wavecheck worktree merge procedure | PASSED | 2026-08-19, host 2.1.235, git 2.51.0. Steps 1–4 execute as specified: disjoint worktrees merge conflict-free in ascending task-id order and the integration smoke passes; a rogue edit colliding with a sibling's file produces a merge conflict, leaves `MERGE_HEAD`, and `git merge --abort` restores `main` with the compliant sibling's work intact — so step 3's "never resolve inline" is satisfiable. **Documented limitation: a clean merge is NOT evidence of ownership compliance.** A rogue edit only conflicts if a sibling happened to touch the same file; a non-colliding unowned edit merges cleanly and lands on `main` silently (verified). The ownership audit is the only defence there — the same shape as [self-audit](self-audit.md) finding 2. **Method: executed mechanically, not agent-driven** (A2 covers live spawning; merging into `main` was kept out of this repo's history). See [verification-log.md](verification-log.md#a2b--post-wavecheck-worktree-merge-procedure). |
| A3 | Orchestrator gate compliance (wavecheck invoked unprompted between waves) | PUBLISHED — **not PASSED** | Updated 2026-08-21 (was 2026-08-19). **28 of 29 wave gates invoked at their boundary, 1 skipped, 27 of 29 recorded before the next wave opened**, across 5 pilot plans (001–005). Gates were invoked on bare "continue"/"go ahead" instructions that named no gate. **The one skip is the most useful data point here, not a blemish:** plan 004's wave-2.0 gate was not run before wave 2.1 opened (deviation 11), the next gate refused to open on the missing prior report, and the retroactive audit then BLOCKed on a *real* ownership breach (deviation 13). The failure path is observed end to end rather than assumed. **Still an upper bound, not a rate, and reaching 5 plans did not change that:** every session knew A3 was measured and one said so aloud at a gate. Plan 005 (2026-09-01) is the weakest row in the ledger, not the strongest — one session planned, executed and audited it — so the sample now touches the bottom of this row's 5–10 bar on its weakest possible instance. Counting that as progress would contradict the argument this row has made since it opened. The bias is not resolved by more plans under the same conditions. PASSED requires one session not told the gate is observed, on a plan it did not plan. Note the 2026-08-19 grep-based recount **cannot detect a skip** — a retroactive report is an ordinary heading — which is why `site/scripts/assert-matrix.mjs` now checks logged skips against the ledger instead. See [a3-gate-compliance.md](a3-gate-compliance.md) and [verification-log.md](verification-log.md#a3--orchestrator-gate-compliance). |
| A4 | `claude plugin validate --strict` | PASSED | 2026-08-18, including `disable-model-invocation` and `isolation` frontmatter. |
| A5 | Browser-drive round trip through Playwright MCP | PASSED | 2026-08-22, host 2.1.235, Node v24.14.1, Chromium 151, against `site/out` served at the basePath by a Node static server (`python3` is not present on this machine, so CLAUDE.md's recipe does not run here). Nine driver capabilities returned live, verified state, each confirmed by a second measurement rather than by the call not erroring: navigate (title matched the export), accessibility snapshot, click on a nav link (`scrollY` 0 → 3346, hash `#evidence`), click on an FAQ disclosure (exactly one `details.open` flipped), `browser_evaluate` (live `--stroke-rule`, `--color-ground`, resolved Inter fallback metrics), network recording on a **real** cross-document navigation (10 requests, all 200, all under `/drydock/`, no external host), resize (390 and 1280, no horizontal overflow at either), multi-tab open/close, and console (0 errors, 0 warnings). This closes the 2026-08-20 limit that only navigate and screenshot had been exercised. **Two stated constraints, neither of them outstanding work.** (a) **Availability is per-session** and is a property of the environment, not of Drydock: the MCP server was absent in one session and present in another on the same machine, and the tool namespace itself moved (`mcp__playwright__browser_*` → `mcp__plugin_playwright_playwright__browser_*`). `drydock:seatrial` HALTs with install instructions when the driver does not resolve, which is the designed response. (b) **`video` evidence is uncapturable through this driver at all** — a per-`BrowserContext` setting fixed at creation, with no video, record or trace tool exposed — which is what produced plan 004's only NO-GO. Form fill is untested because this target has zero `<form>` and zero inputs, measured, not assumed. See [verification-log.md](verification-log.md#a5--playwright-mcp-availability-and-browser-drive-round-trip). |
| A6 | Ownership enforcement hook fires in a live session | PASSED | Updated 2026-08-22 (was LOGIC VERIFIED — live registration PENDING, 2026-08-21). Host 2.1.235, Node v24.14.1, plugin cache 0.7.0 at `d7de845`. **The host invokes the hook and a real edit was denied**, measured by a session that wrote none of this code: with the wave armed by `wave-start` from plan 004 wave 2.1, a `Write` and a valid `Edit` to unowned paths were both refused with the hook's verbatim payload and left the files untouched, while a `Write` and an `Edit` inside `owns` were allowed. After `rm .drydock/wave-owns.json` the same denied write succeeded — inert again within the one session, because the config is read per invocation while registration is fixed at session start. `.drydock/enforcement.log` held exactly 4 lines, 2 deny and 2 allow, one per hook-mediated decision and none for the rest. The 12-case logic self-check is unchanged and still passes. **Both ceilings stand and were exercised rather than assumed:** **Bash-mediated writes bypass file-tool hooks entirely** — a `>` redirect to the same denied path wrote and logged nothing — and paths outside the project directory are not enforced; `audit-wave` is the backstop for both. `NotebookEdit` is covered by the matcher and by the logic cases but has no live denial: this repo has no notebooks.  **Second live denial, 2026-09-01, plan 005 wave 1.0 (v0.8.4):** 13 hook decisions recorded — 12 allow, 1 **deny**. The deny refused an edit to the plan document while the wave was armed, which is the exact mixture that produced plan 004's deviation 13 (`5a32ac9` staged the plan document alongside a task's owned file) — refused at the tool boundary this time rather than caught by a retroactive audit. **Weaker than the 2026-08-22 run on independence** (the session that triggered the deny had written the code) and **the raw receipt no longer exists**: `.drydock/` is gitignored and was removed during that session, so the durable record is the committed wavecheck report in the plan, not the log file. Counted as a second occasion, not as stronger evidence. See [verification-log.md](verification-log.md#a6--ownership-enforcement-hook-pretooluse). |
| A7 | `seatrial` Testing Gate executes end to end | OBSERVED — two full runs | Run 2026-08-20 and again 2026-08-26 (updated), both through Playwright MCP against `site/out` served at the basePath. Six declared cases TG1–TG6 ran to a verdict sheet both times with **identical verdicts**: 3 PASS, 3 FAIL, **and all three failures were the designed ones**. TG2 failed against a deliberately false expectation instead of agreeing with it; TG3 returned the exact reason string `step not executable`, clicked no substitute element, and **halted and asked**; TG4's assertion held while its `video` evidence clause failed, which is a harness limit (A5) rather than a site defect. Summary verdict **NO-GO** both times — seatrial never writes an override for itself. **The second run is the stronger evidence**, because it hit three refusals the first never reached: it HALTed on stale cases (12 commits had moved `site/` since the cases' baseline) and continued only on a human re-validation; it HALTed on an unreachable target rather than starting the app; and when the driver disconnected mid-suite it wrote **no partial sheet and no specs**, with two other browser drivers available in-session and neither used. Regenerating the specs from the second run reproduced them byte-identical. **The generated specs now run, and pass.** First executed 2026-09-02 in `.github/workflows/verify.yml`: **6 passed (8.7s)**, with TG2 and TG3 as `test.fail()` inversions, so their passing means they failed exactly as designed. They were generated at commit `5a32ac9` and are still green against an export many releases later — repeatability measured rather than assumed. `@playwright/test` remains absent from this repo's dependencies and seatrial remains forbidden from adding it; CI installs it into a scratch directory outside the tree. **Stated limits:** one browser (**Chromium only**), one viewport, one target — that is not a compatibility rate. Click, form fill and mutation, multi-tab, mobile viewports and non-Chromium engines are untested. See [verification-log.md](verification-log.md#a7--seatrial-testing-gate-end-to-end-plan-004-tg1tg6). |
| — | Contract logic (audit soundness, BLOCK path, attribution) | VERIFIED | Adversarial dry-run — see [self-audit.md](self-audit.md). Defect found and fixed in v0.3.0. |

Public release criteria — **3 of 4 met, 1 partial**:

- ✅ A1 and A2 resolved to one-line compatibility notes.
- ✅ One example plan ([001-drydock-homepage](plans/001-drydock-homepage.md)) and
  one field case study ([case-study-001-homepage](case-study-001-homepage.md)).
- ⚠️ **A3 published with real numbers — half met, and deliberately stopped there.**
  The numbers are real and published (28 of 29 gates invoked at their boundary
  across 5 plans, including one skipped gate that the following gate caught). The
  sample has reached the bottom of the 5–10 this row asks for, on its least
  independent instance — plan 005 was planned, executed and audited by one session
  — and every run was observed, so the figure is an upper bound rather than a rate.
  Continuing under the same conditions fixes the count and not the meaning, which
  is why measurement stopped at 5 rather than being extended. Still half met: the
  half that is missing is independence, not sample size.
- ✅ **A2b verified** (2026-08-19, mechanically — see the row above for the method
  limit and the clean-merge caveat).

Licensed MIT as of v0.4.0. Remaining before public release is therefore evidence,
not code.

**Cost:** what one full run costs is published in [cost-001.md](cost-001.md) —
including the number that run never captured. Token spend was not instrumented,
so the economy claim in the plugin description remains a design intent rather
than a measured result, and the page says so rather than estimating.

## 2026-08-20 — published as an open pilot

The homepage went live at <https://sandeeptakasi.github.io/drydock/>
and the repo went public with **A3 unresolved and stated on the page**, rather
than holding the release until it passes.

**No row status changed to make that happen.** Going public is a decision, not
evidence, and this file moves only on evidence — see
[verification-log.md](verification-log.md). The release criteria above still read
3 of 4 met, 1 partial, because that is still true.

What ships visible instead: the site renders A3 as `PUBLISHED, NOT PASSED` with
"ceiling, not a rate" attached, and `scripts/assert-copy.mjs` now **requires**
both of those strings. Before this, nothing forced the caveat to appear — the
page could have promoted A3 and the gate would still have gone green. Proven
failable the same day: doctoring the export to say `PASSED` with the caveat
removed fails `assert-copy` naming both literals.

The status line reads "open pilot -- field benchmarks pending" rather than
"internal pilot", which was simply false on a public URL.

A3's route to PASSED is unchanged: one session not told the gate is observed, on
a plan it did not plan.
